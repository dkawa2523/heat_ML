project:
  seed: 42
  run_name: graph_rc_topcell_v0
  output_dir: outputs/runs
  overwrite_run: true

data:
  raw_dir: data/raw
  file_pattern: "temp_*.csv"
  # Allows suffixes such as temp_20_120_100_recipe.csv while still extracting representative conditions.
  filename_regex: 'temp_(?P<brine>[-+0-9.eE]+)_(?P<heater>[-+0-9.eE]+)_(?P<plasma>[-+0-9.eE]+)(?:_.*)?\.csv'
  columns: [time, CP, Center, middle, edge]
  sensor_cols: [CP, Center, middle, edge]
  control_cols: [brine, heater, plasma]
  sep: "[\\s,]+"
  header: auto
  dt: 1.0
  temp_min: -20.0
  temp_max: 300.0

split:
  # choices: random, holdout_max, holdout_min, holdout_corner
  method: random
  train_ratio: 0.70
  val_ratio: 0.15
  test_ratio: 0.15
  # Example for plasma extrapolation benchmark:
  # method: holdout_max
  # holdout_control: plasma

features:
  history_steps: 8
  target: delta_temperature
  use_effective_controls: true
  control_lag_tau:
    brine: 4.0
    heater: 8.0
    plasma: 1.0

model:
  # choices: linear_rc, mlp, cnn1d, gru, lstm, tcn, thermal_state_space, graph_rc
  name: graph_rc
  hidden_dim: 32
  dropout: 0.05
  residual_scale: 0.03
  graph:
    node_table: configs/cell_nodes.csv
    edge_table: configs/cell_edges.csv
    symmetric: true
    init_from_resistance: true
    learn_conductance: true
    ignore_unknown_edges: false
    conduction_scale: 0.05
    use_source_prior: true
    source_prior_scale: 0.05
    control_signs:
      brine: -1.0
      heater: 1.0
      plasma: 1.0

train:
  epochs: 60
  batch_size: 256
  learning_rate: 0.001
  weight_decay: 0.00001
  patience: 15
  gradient_clip: 1.0
  print_every: 10
  device: auto
  num_threads: 1
  rollout_loss_weight: 0.0
  rollout_loss_steps: 3
  graph_prior_loss_weight: 0.001

evaluation:
  make_plots: true
  max_plots_per_split: 3
  dynamic_case_keywords: [step, ramp, pulse, recipe, schedule]

compare:
  runs_dir: outputs/runs
  output: outputs/model_leaderboard.csv
